.venv/
runs/
crops/
captures/
# derived by coffeecam.fullness_dataset, rebuilt on demand
fullness_dataset/
# derived by coffeecam.annotation_audit, rebuilt on demand
annotation_audit.csv
audit_contact/
dataset/images/*
!dataset/images/kahvi.png
!dataset/images/*_shift_x*_y*.png
dataset/previews/*
!dataset/previews/*_shift_x*_y*_preview.png
__pycache__/
*.pyc
*.pt
*.cache
.pytest_cache/

# working detritus (logs, backups, compare outputs)
scratchpad/

# committed model weights (exception to *.pt above)
!models/*.pt
# ...but not this one — local-only, superseded by best-trackB-v1.pt
models/best-balanced-v1.pt
# derived by scratchpad/ds_summary.py
dataset_summary/

# real homelab hosts/IPs — see hosts.env.example for the committed template
hosts.env
