# Copy to hosts.env (gitignored) and fill in real homelab addresses.
# coffeecam/hosts.py loads this at import time; env vars already set win.

COFFEECAM_SOURCE_URL=http://coffeecam-camera.example:8888
